#include "WindowBorderPanelWrapper.h"

#include "WindowBorderPanel_mac.h"

WindowBorderPanel *CreateWindowBorderPanel(const std::string& window_number, int32_t timer_interval_ms)
{
	return new WindowBorderPanelMac(window_number, timer_interval_ms);
}
